export function transitionReducer(state) {
  /* if (shouldRedirectToLogin(state)) {
    return toLogin()(state);
  }

  if (shouldRedirectToDefaultView(state)) {
    return fromLogin()(state);
  }*/

  return state;
}
